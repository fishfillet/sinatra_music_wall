<h1>Songs and Shit</h1>

<% @songs.each do |song| %>
  <div class="song">
    <h3><a href="/songs/<%= song.id %>"><%= song.song_title %></a> 
    <small>(by: <%= song.author %>)</small></h3>
    <h4><a href="<%= song.url %>">Link</a></h4>
  </div>
<% end %>

<a href="/songs/new">Post a song and see shit!</a>  
